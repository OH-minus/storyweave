Score how closely the shared story matches the private reference story in characters, setting, important events, themes,
and outcome. Judge meaning rather than exact wording. Return a JSON object, containing only one key called "value" of
type integer from 0 (unrelated) to 100 (very close).

PRIVATE REFERENCE STORY:
{{referenceStory}}

SHARED STORY:
{{sharedStory}}